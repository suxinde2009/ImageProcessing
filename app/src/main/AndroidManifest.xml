<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jackie.processing.image">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.PrimaryColorActivity"/>
        <activity android:name=".ui.ColorMatrixActivity"/>
        <activity android:name=".ui.PixelsEffectActivity"/>

        <activity android:name=".ui.MatrixActivity"/>
        <activity android:name=".ui.RoundRectXfermodeActivity"/>
        <activity android:name=".ui.BitmapShaderActivity"/>
    </application>

</manifest>
